[project]
name = "libredevops-architecture"
version = "0.1.0"
description = "Diagram-as-code sources for libredevops.org architecture diagrams."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "diagrams>=0.24.4",
]
